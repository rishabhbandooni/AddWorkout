// src/components/WorkoutLog.js
import React from "react";
import { Container, Typography } from "@mui/material";

const WorkoutLog = () => {
  return (
    <Container>
      <Typography variant="h2">Workout Log</Typography>
      {/* Display workout log entries */}
    </Container>
  );
};

export default WorkoutLog;
